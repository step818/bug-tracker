import React, { Component } from 'react';

class AddMember extends Component {
  render() {
    return(
      <div>
        Add Member Search Form:
          Search by email or username
          Send them an invite and notification
            of the invite.
      </div>
    );
  }
};

export default AddMember;